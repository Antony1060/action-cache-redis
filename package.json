{
  "name": "cache-redis",
  "module": "src/restore.ts",
  "type": "module",
  "scripts": {
    "build": "ncc build -o dist/restore src/restore.ts && ncc build -o dist/save src/save.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/glob": "^0.4.0",
    "redis": "^4.6.12"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  }
}
